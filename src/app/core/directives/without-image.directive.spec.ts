import { WithoutImageDirective } from './without-image.directive';

describe('WithoutImageDirective', () => {
  it('should create an instance', () => {
    const directive = new WithoutImageDirective();
    expect(directive).toBeTruthy();
  });
});
